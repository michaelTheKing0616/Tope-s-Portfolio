{
  "name": "kobo",
  "version": "0.2.0",
  "description": "Invoicing and payments for African freelancers — money in kobo, idempotent webhooks, append-only audit log.",
  "type": "module",
  "license": "MIT",
  "author": "Temitope Olaitan",
  "scripts": {
    "dev": "tsx src/bin.ts",
    "start": "node dist/server.js",
    "build": "tsc -p tsconfig.build.json",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@hono/node-server": "^1.13.7",
    "hono": "^4.6.14"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "tsx": "^4.19.2",
    "typescript": "^5.6.3",
    "vitest": "^2.1.8"
  }
}

[build]
  command = "npm install && npm run build"
  publish = "apps/web/dist"

[build.environment]
  NODE_VERSION = "20"

# Set in Netlify UI after API is deployed:
# VITE_API_URL = "https://your-sportverse-api.onrender.com"
